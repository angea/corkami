"""check if TLS was added
check if import was added
check if EP is the same
check if code at EP is the same
try to check if internal calls are the same
try to check if import trampolines are the same
"""